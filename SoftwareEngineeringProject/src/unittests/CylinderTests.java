/**
 * 
 */
package unittests;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

/**
 * 
 */
class CylinderTests {

	/**
	 * Test method for {@link geometries.Cylinder#getNormal(primitives.Point)}.
	 */
	@Test
	void testGetNormal() {
		fail("Not yet implemented");
	}
/*
 * For those who create a getNormal for finite cylinder – there are 3 equivalence tests (on the side
and on each one of the bases) and two boundaries – the center of the bases. (There is no need
to check points on the connection between bases and side of tube, since no points like this will
be created in the findIntersections method).
 * 
 * */
}
